apiVersion: v1
kind: Service
metadata:
  name: {{ include "linkhub-backend.fullname" . }}
  labels:
    app: {{ include "linkhub-backend.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "linkhub-backend.name" . }}
